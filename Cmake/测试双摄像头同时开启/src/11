#include<opencv2/opencv.hpp>
#include<stream>
using namespace cv;
int main()
{
    VideoCapture capture(0)
    {
        Mat frame;
        capture>>frame;
        imshow("1",frame);
        waitKey(1);
    }
    return 0;
}